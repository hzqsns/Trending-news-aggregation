# API 认证 Token（首次启动会自动生成，也可以手动指定）
API_TOKEN=

# 数据库（默认 SQLite 本地文件）
DATABASE_URL=sqlite+aiosqlite:///./data/news_agent.db

# AI 配置（通过 Web 页面也可以修改）
AI_API_KEY=
AI_API_BASE=https://api.openai.com/v1
AI_MODEL=gpt-4o-mini

# Telegram（通过 Web 页面也可以修改）
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# 微信 PushPlus（通过 Web 页面也可以修改）
PUSHPLUS_TOKEN=

# QQ Qmsg（通过 Web 页面也可以修改）
QMSG_KEY=
